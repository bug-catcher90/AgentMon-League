{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm exec prisma generate && pnpm run build && cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/",
    "watchPatterns": ["src/**", "prisma/**", "package.json", "next.config.mjs"]
  },
  "deploy": {
    "startCommand": "cd .next/standalone && node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60
  }
}
